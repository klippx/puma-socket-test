This repo reproduces an errors encountered when performing a puma phased
restart. To run the test:

```
bundle install
./test.sh
```

Note that this was created to run on OS X. `ab` flags may differ by platform.
